# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'obi' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for obi
  
  pod 'Firebase'

  target 'obiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'obiUITests' do
    # Pods for testing
  end

end
